spring:
  application:
    name: "nbcamp-lecture-spring-security"
  datasource:
    url: "jdbc:mariadb://localhost:33301/auth"
    username: root
    password: password
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true

logging:
  level:
    root: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE
    org.springframework.security: DEBUG
    reactor:
      netty: DEBUG

jwt:
  secret: "ccv36zwc434spf0x7s4kucgb2w9straidcrparn2yhdbkwrhzle4vwkmac1400om"
  expiry-milli: 3600000